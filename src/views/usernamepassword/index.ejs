
    <% if (locals.isFailedLogin) { %>
    <div class="row">
        <div class="col-12">
            <div class="notification notification-error">
                <%= locals.message || 'error!' %>
            </div>
        </div>
    </div>
    <% } %>
    <div class="grid-row">
        <div class="col-9">
            <h1 class="heading-xlarge">
                Sign in to access DfE services
            </h1>
        </div>
    </div>
    <div class="row">
        <div class="col-8">
            <form method="post">
                <input type="hidden" name="_csrf" value="<%=csrfToken%>" />
                <fieldset>
                    <legend class="vh">Sign in form</legend>
                    <div class="form-group">
                        <label class="form-label-bold" for="username">Email address</label>
                        <% if(locals.emailValidationMessage!== undefined || locals.emailValidationMessage !== '') { %>
                            <label class="form-label-bold" for="username_error"><%=locals.emailValidationMessage %></label>
                        <% } %>
                        <input class="form-control" id="username" name="username" type="email">
                    </div>
                    <div class="form-group">
                        <label class="form-label-bold" for="password">Password</label>
                        <% if(locals.passwordValidationMessage !== undefined || locals.passwordValidationMessage !== '') { %>
                        <label class="form-label-bold" for="password_error"><%=locals.passwordValidationMessage%></label>
                        <% } %>
                        <input class="form-control" id="password" name="password" type="password">
                    </div>
                    <div class="panel">
                        <p>By continuing you accept the <a href="#">terms and conditions</a>.</p>
                    </div>
                    <div class="form-submit">
                        <button type="submit" class="button">Sign in</button>
                    </div>
                </fieldset>
            </form>
        </div>
        <div class="col-4">
            <aside>
                <h2 class="heading-medium">Problems signing in?</h2>
                <nav class="subnav">
                    <ul>
                        <li><a href="#">Forgotten your password?</a></li>
                    </ul>
                </nav>
            </aside>
        </div>
    </div>

