<div class="grid-row">
    <div class="col-8">
        <h1 class="heading-xlarge">
            Your email address
        </h1>
        <p class="lede">To access your services through DfE Sign-in your email address will become your username.</p>
        <p>If your email address is not private or you don't have access to it you will need to change it. </p>
        <form method="post" action="email">
            <input type="hidden" name="_csrf" value="<%=csrfToken%>" />
            <input type="hidden" name="viewToDisplay" value="<%=locals.viewToDisplay%>" />
            <div class="form-group">
                <fieldset>

                    <legend>
                        <h1 class="heading-medium">
                            Which email address do you want to use?
                        </h1>
                    </legend>

                    <div class="multiple-choice">
                        <input id="current-email" type="radio" name="radioEmailGroup" value="Yes" <% if (!locals.validationMessages.email !== undefined) { %>checked<%}%>>
                        <label for="current-email"><%=locals.user.email%></label>
                    </div>

                    <div class="multiple-choice" data-target="new-email-address">
                        <input id="new-email" type="radio" name="radioEmailGroup" value="No" <% if (locals.validationMessages.email !== undefined) { %>checked<%}%>>
                        <label for="new-email">Use a different email address</label>
                    </div>

                    <div class="panel panel-border-narrow js-hidden form-group  <%= locals.validationMessages.email !== undefined ? "form-group-error" : "" %>" id="new-email-address">
                        <label class="form-label" for="email">New email address</label>
                        <p class="form-hint">We will send you an email to confirm your new address </p>
                        <% if (locals.validationMessages.email !== undefined) { %>
                        <p class="error-message" id="validation-code"><%= locals.validationMessages.email %></p>
                        <% } %>
                        <input class="form-control" name="email" type="text" id="email">
                    </div>

                </fieldset>
            </div>
            <div class="form-submit">
                <button type="submit" class="button">Continue</button>
            </div>
        </form>

    </div>
</div>