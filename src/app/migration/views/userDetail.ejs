<div class="js-back-link"></div>

<% if (locals.failedValidation) { %>
    <div class="notification notification-information with-icon">
        <h2>Your organisation details do not exist </h2>
        <p>We have found your account details from Secure Access.
            Unfortunately the details of the organisation you work for have not been created in our system
            so we cannot continue with your DfE Sign-in account setup. <a href="<%=locals.urls.help%>/contact">Contact the help desk</a> for assistance.</p>
    </div>
<%}%>

<div class="grid-row">
    <div class="col-8">
        <h1 class="heading-xlarge">
            Your details
        </h1>
    </div>
</div>
<div class="grid-row">
    <div class="col-6">
        <p>We have identified your account from Secure Access.</p>
        <dl class="rows spacer la">
            <dt>Name</dt>
            <dd><%=locals.user.firstName%> <%=locals.user.lastName%></dd>
            <dt>Email address</dt>
            <% if(locals.user.email) { %>
                <dd><%=locals.user.email%></dd>
            <%} else { %>
                <dd><em>No email address found</em></dd>
            <%}%>
            <dt>Organisation</dt>
            <dd>
                <%=locals.user.organisation.name%>
                <% if(locals.user.organisation.type === '001') { %>
                    URN: <%=locals.user.organisation.urn%>
                <%} else if(locals.user.organisation.type === '010' || locals.user.organisation.type === '013') { %>
                    UID: <%=locals.user.organisation.uid%>
                <%} %>
            </dd>
        </dl>
        <% if (!locals.failedValidation) { %>
            <p><a href="/<%=locals.uuid%>/migration/email" class="button">Continue</a> </p>
        <%}%>
    </div>
</div>
