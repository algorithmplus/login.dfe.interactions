

    <div class="grid-row">
        <div class="col-8">
            <h1 class="heading-xlarge">
                Sign in
            </h1>
        </div>
    </div>
    <div class="row">
        <div class="col-12">
            <form method="post" id="form-signin">
                <input type="hidden" name="_csrf" value="<%=csrfToken%>" />
                <fieldset>
                    <legend class="vh">Sign in form</legend>
                    <div class="form-group <%= (locals.validationMessages.username !== undefined) ? 'form-group-error' : '' %>">
                        <label class="form-label" for="username">Email address</label>
                        <% if (locals.validationMessages.username !== undefined) { %>
                            <p class="error-message" id="validation-email"><%=locals.validationMessages.username %></p>
                        <% } %>
                        <input class="form-control" id="username" name="username" type="email" <% if (locals.validationMessages.username !== undefined) { %> aria-invalid="true" aria-describedby="validation-email" <% } %> value="<%=locals.username%>">
                    </div>
                    <div class="form-group <%= (locals.validationMessages.password !== undefined) ? 'form-group-error' : '' %>">
                        <label class="form-label" for="password">Password</label>
                        <% if(locals.validationMessages.password !== undefined) { %>
                            <p class="error-message" id="validation-password"><%=locals.validationMessages.password%></p>
                        <% } %>
                        <div class="show-password">
                            <input class="form-control" id="password" name="password" type="password" <% if (locals.validationMessages.password !== undefined) { %> aria-invalid="true" aria-describedby="validation-password" <% } %>>
                        </div>
                        <p class="help-text"><a href="/<%=locals.uuid%>/resetpassword/request?clientid=<%=locals.clientId%>&redirect_uri=<%=locals.redirectUri%>">I've forgotten my password</a></p>
                    </div>
                    <div class="panel panel-border-wide">
                        <p>By continuing you accept the <a href="<%= locals.portal.url %>/terms">terms and conditions</a>.</p>
                    </div>
                    <div class="form-submit submit-buttons">
                        <button type="submit" class="button">Sign in</button>
                        <button type="submit" name="cancel" value="true" class="button button-secondary">Cancel</button>
                    </div>
                </fieldset>
            </form>
        </div>
    </div>
    <div class="row">
        <div class="col-8">
            <%- include('../../includes/helpdesk'); %>.
        </div>
    </div>

